import isValid from"../isValid/index.js";import isWeekend from"../isWeekend/index.js";import toDate from"../toDate/index.js";import differenceInCalendarDays from"../differenceInCalendarDays/index.js";import addDays from"../addDays/index.js";import isSameDay from"../isSameDay/index.js";import toInteger from"../_lib/toInteger/index.js";export default function differenceInBusinessDays(dirtyDateLeft,dirtyDateRight){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var dateLeft=toDate(dirtyDateLeft),dateRight=toDate(dirtyDateRight);if(!isValid(dateLeft)||!isValid(dateRight))return new Date(NaN);var calendarDifference=differenceInCalendarDays(dateLeft,dateRight),sign=0>calendarDifference?-1:1,weeks=toInteger(calendarDifference/7),result=5*weeks;dateRight=addDays(dateRight,7*weeks);while(!isSameDay(dateLeft,dateRight)){result+=isWeekend(dateRight)?0:sign;dateRight=addDays(dateRight,sign)}return 0===result?0:result}